# History Service (Flask) — Day 7 (D7-YL-2)

This service records a logged-in user’s browsing history for **Published** posts only.

## Run locally
```bash
python -m venv .venv
Mac:
source .venv/bin/activate
Win:
.\.venv\Scripts\activate
pip install -r requirements.txt
